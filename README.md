# BalatroPGDCModpack

----------------------------------
Color Values
----------------------------------

G.C = {
    MULT       = HEX('FE5F55'),   -- coral red — same value as RED
    XMULT      = HEX('FE5F55'),   -- identical to MULT
    CHIPS      = HEX('009dff'),   -- bright blue — same value as BLUE
    MONEY      = HEX('f3b958'),   -- warm gold
    FILTER     = HEX('ff9a00'),   -- identical to IMPORTANT
    IMPORTANT  = HEX('ff9a00'),   -- orange
    BLUE       = HEX('009dff'),
    RED        = HEX('FE5F55'),
    GREEN      = HEX('4BC292'),
    PALE_GREEN = HEX('56a887'),
    ORANGE     = HEX('fda200'),
    GOLD       = HEX('eac058'),
    YELLOW     = {1, 1, 0, 1},     -- pure yellow, not a hex string
    CLEAR      = {0, 0, 0, 0},     -- fully transparent
    WHITE      = {1, 1, 1, 1},
    PURPLE     = HEX('8867a5'),
    BLACK      = HEX('374244'),
    L_BLACK    = HEX('4f6367'),   -- a lighter black/slate
    GREY       = HEX('5f7377'),
    CHANCE     = HEX('4BC292'),   -- identical to GREEN
    -- (table continues further — JOKER_GREY and others exist beyond
    -- what's shown here, plus nested groups like G.C.SECONDARY_SET)
}
